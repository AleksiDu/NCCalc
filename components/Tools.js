/**
 * Set yp the Tools class
 */

class Tools {
    constructor(
        id,
        name,
        type,
        diameter,
        feed,
        speed,
    ) {
        this.id = id;
        this.name = name;
        this.type = type;
        this.diameter = diameter;
        this.feed = feed;
        this.speed = speed;
    }
}

export default Tools;
